import React from 'react';
import './styles.css'

const Store = (props) => {
    return (
        <div className="app__store app-store">
            <h2 className="app-store__title">Two Components - One Store for their States</h2>
        </div>
    );
};

export default Store;